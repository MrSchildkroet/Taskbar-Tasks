!prio='high'|'medium'|'low' -> Priorität festlegen, wird als Metadaten in die DB gespeichert

- [] -> Fertig/Nicht-fertig Toggle

#type='chore'|'bug'|'documentation' -> Type der Task, erscheint als Badge

@date -> ISO String, Due Date.
